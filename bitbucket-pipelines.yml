options:
  docker: true
definitions:
  services:
    docker:
      memory: 3072
    docker-large:
      memory: 6144
      type: docker
  steps:
    - step: &build-image
        image: node:16
        name: Build Application
        size: 2x
        deployment: test
        script:
          - echo $SSH_USER

clone:
  depth: full
pipelines:
  branches:
    master:
      - step: *build-image
